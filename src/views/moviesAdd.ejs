<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie add</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>
    <h1 class="titulo">Agregar Película</h1>
    <br>
    <section class="formulario">
        <form action="/movies/create" method="POST">
            <label for="title">Title </label>
            <p><input type="text" name="title" id="title" value="<%= locals.old && (old.title && old.title) %>"></p>
            <p><small class="small_danger"><%= locals.errors && (errors.title && errors.title.msg) %></small></p>
            
            <label for="rating">Rating </label>
            <p><input type="number" name="rating" id="rating" value="<%= locals.old && (old.rating && old.rating) %>"></p>
            <p><small class="small_danger"><%= locals.errors && (errors.rating && errors.rating.msg) %></small></p>
            
            <label for="awards">Awards </label>
            <p><input type="number" name="awards" id="awards" value="<%= locals.old && (old.awards && old.awards) %>"></p>
            <p><small class="small_danger"><%= locals.errors && (errors.awards && errors.awards.msg) %></small></p>
            
            <label for="release_date">Release date </label>
            <p><input type="date" name="release_date" id="release_date" value="<%= locals.old && (old.release_date && old.release_date) %>"></p>
            <p><small class="small_danger"><%= locals.errors && (errors.release_date && errors.release_date.msg) %></small></p>
            
            <label for="length">Length </label>
            <p><input type="number" name="length" id="length" value="<%= locals.old && (old.length && old.length) %>"></p>
            <p><small class="small_danger"><%= locals.errors && (errors.length && errors.length.msg) %></small></p>
            
            <br>
            <p> <button class="botonAgregar" type="submit">Agregar</button>
            
            <a  class="botonVolver" href="/movies">Listado de Películas</a></p>
        </form>
    </section>
    
</body>

</html>